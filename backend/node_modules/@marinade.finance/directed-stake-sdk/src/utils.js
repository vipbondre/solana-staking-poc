"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.debugStr = void 0;
/**
 * Returns a string representation Transaction object.
 * As of
 * https://github.com/saber-hq/saber-common/blob/v1.14.11/packages/solana-contrib/src/transaction/TransactionEnvelope.ts#L420
 */
function debugStr(tx) {
    return [
        '=> Instructions',
        tx.instructions
            .map((ser, i) => {
            return [
                `Instruction ${i}: ${ser.programId.toString()}`,
                ...ser.keys.map((k, i) => `  [${i}] ${k.pubkey.toString()} ${k.isWritable ? '(mut)' : ''} ${k.isSigner ? '(signer)' : ''}`),
                `  Data (base64): ${ser.data.toString('base64')}`,
            ].join('\n');
        })
            .join('\n'),
        '=> Signers',
        tx.signatures.map(sg => sg.publicKey.toString()).join('\n'),
    ].join('\n');
}
exports.debugStr = debugStr;
//# sourceMappingURL=utils.js.map