import { PublicKey } from '@solana/web3.js';
export declare function voteRecordAddress({ directedStakeProgramId, root, owner, }: {
    directedStakeProgramId?: PublicKey;
    root: PublicKey;
    owner: PublicKey;
}): {
    address: PublicKey;
    bump: number;
};
