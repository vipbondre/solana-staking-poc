import { PublicKey, TransactionInstruction } from '@solana/web3.js';
import { DirectedStakeSdk } from './sdk';
/**
 * Generating an instruction to create a new root account
 * of directed stake contract.
 *
 * Instruction requires signature of root and rentPayer.
 *
 * @type {Object} args - Arguments on instruction creation
 * @param {DirectedStakeSdk} args.sdk - Directed stake SDK instance
 * @param {PublicKey} args.root - Address of the directed stake root account that will be created
 * @param {PublicKey} args.rentPayer - Payer of the rent excempt price the root account is created with
 *                                    (when not passed as the argument the sdk wallet will be taken as the payer)
 * @type {Object} return - Return data of generated instruction
 * @return {TransactionInstruction} return.instruction - Instruction to create a new root account
 */
export declare function withCreateRoot({ sdk, root, rentPayer, }: {
    sdk: DirectedStakeSdk;
    root: PublicKey;
    rentPayer?: PublicKey;
}): Promise<{
    instruction: TransactionInstruction;
}>;
