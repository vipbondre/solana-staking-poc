import { ProgramAccount } from '@coral-xyz/anchor';
import { PublicKey } from '@solana/web3.js';
import { DirectedStakeRoot, DirectedStakeSdk, DirectedStakeVoteRecord } from './sdk';
export declare function voteRecord({ sdk, address, }: {
    sdk: DirectedStakeSdk;
    address: PublicKey;
}): Promise<DirectedStakeVoteRecord>;
export declare function root({ sdk, address, }: {
    sdk: DirectedStakeSdk;
    address: PublicKey;
}): Promise<DirectedStakeRoot>;
export declare function findVoteRecords({ sdk, root, owner, target, }: {
    sdk: DirectedStakeSdk;
    root?: PublicKey;
    owner?: PublicKey;
    target?: PublicKey;
}): Promise<ProgramAccount<DirectedStakeVoteRecord>[]>;
