import { Connection, PublicKey } from "@solana/web3.js";
import BN from "bn.js";
export declare const DEFAULT_STAKE_AUTHORITY: PublicKey;
export declare const DEFAULT_EXIT_AUTHORITY: PublicKey;
export declare const DEFAULT_BENEFICIARY: PublicKey;
export declare const DEFAULT_NATIVE_STAKING_API_URL = "https://native-staking.marinade.finance";
export declare const NATIVE_STAKING_API_URL_DEV = "https://native-staking-dev.marinade.finance";
export declare const NATIVE_STAKING_REFERRAL_API_URL = "https://ns-referral.marinade.finance";
export declare const DEFAULT_PREPARE_FOR_REVOKE_COST: BN;
export declare class NativeStakingConfig {
    connection: Connection;
    stakeAuthority: PublicKey;
    exitAuthority: PublicKey;
    nativeStakingApiUrl: string;
    nativeStakingReferralApiUrl: string;
    beneficiary: PublicKey;
    prepareForRevokeCost: BN;
    constructor(overrides?: Partial<NativeStakingConfig>);
}
export declare const DEFAULT_NATIVE_STAKING_SELECT_API_URL = "https://ns-prime.marinade.finance";
export declare const NATIVE_STAKING_SELECT_API_URL_DEV = "https://ns-prime-dev.marinade.finance";
export declare class NativeStakingSelectConfig {
    connection: Connection;
    stakeAuthority: PublicKey;
    exitAuthority: PublicKey;
    nativeStakingApiUrl: string;
    nativeStakingReferralApiUrl: string;
    beneficiary: PublicKey;
    prepareForRevokeCost: BN;
    constructor(overrides?: Partial<NativeStakingConfig>);
}
